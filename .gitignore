node_modules
dist
.next
# Written by `next build`/`next dev`; its references point into .next, which is ignored too.
next-env.d.ts
.turbo
*.tsbuildinfo
.env
.env.local
coverage
test-results
playwright-report
docs/dependency-graph.svg

# Kontrakt úkolu agenta (agent-rig). Žije jen ve worktree, nikdy se necommituje.
# Musí být tady, ne v .git/info/exclude: prettier a spol. čtou .gitignore.
.task/
